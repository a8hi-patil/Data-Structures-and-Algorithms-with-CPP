#include <iostream>
#include <cstdlib>
#include <stack>
using namespace std;
int main()
{
    system("cls");
    stack<int> st;
    st.push(1);
    st.size();
    st.top();
    st.empty();
    return 0;
}