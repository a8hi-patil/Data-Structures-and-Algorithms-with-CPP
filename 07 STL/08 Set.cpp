#include <iostream>
#include <set>
#include <cstdlib>
using namespace std;
int main()
{
    system("cls");
    set<int> st;
    // unorderd_set<int> st;
    st.insert(9);
    st.end();
    st.find(3);
    st.begin();
    st.size();
    return 0;
}